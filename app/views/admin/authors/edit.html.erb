<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>✏️ 작가 수정: <%= @author.name %></h1>
    <%= link_to "← 목록", admin_authors_path, class: "btn btn-outline-secondary" %>
  </div>

  <div class="card">
    <div class="card-body">
      <%= form_with model: [:admin, @author], class: 'row g-3' do |f| %>
        <div class="col-md-6">
          <%= f.label :name, '이름 *', class: 'form-label' %>
          <%= f.text_field :name, class: 'form-control', required: true %>
        </div>

        <div class="col-md-6">
          <%= f.label :role, '역할 *', class: 'form-label' %>
          <%= f.select :role,
            options_for_select([
              ['작가', 'writer'],
              ['일러스트레이터', 'illustrator'],
              ['성우', 'narrator'],
              ['제작자', 'producer']
            ], @author.role),
            {},
            class: 'form-select',
            required: true
          %>
        </div>

        <div class="col-12">
          <%= f.label :bio, '소개', class: 'form-label' %>
          <%= f.text_area :bio, rows: 4, class: 'form-control' %>
        </div>

        <div class="col-md-8">
          <%= f.label :profile_image, '프로필 이미지 URL', class: 'form-label' %>
          <%= f.url_field :profile_image, class: 'form-control' %>
        </div>

        <div class="col-md-4">
          <div class="form-check mt-4 pt-2">
            <%= f.check_box :active, class: 'form-check-input' %>
            <%= f.label :active, '활성 상태', class: 'form-check-label' %>
          </div>
        </div>

        <div class="col-12">
          <div class="alert alert-info">
            <strong>연결된 코스:</strong> <%= @author.courses.count %>개
          </div>
        </div>

        <div class="col-12">
          <%= f.submit '💾 저장', class: 'btn btn-primary' %>
          <%= link_to '취소', admin_authors_path, class: 'btn btn-outline-secondary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
